const MinigameType = {};
const MinigameTypeMappings = {};

export default class Minigame {

  constructor(name) {
    this.name = name;
  }

  start() {
  }

  stop() {
  }

  handlePacket(packet) {
  }

  renderHook() {
  }
}

export {MinigameType, MinigameTypeMappings};